process {
    executor = 'slurm'
    queue = 'cmobic_cpu'
}

process {
    errorStrategy = 'retry'
    maxRetries = 3
    errorStrategy = { task.attempt <= process.maxRetries ? 'retry' : 'terminate' }
}

env {
  SPARK_LOCAL_DIRS = './'
}